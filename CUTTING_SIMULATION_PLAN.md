# リアルタイム切削シミュレーション - 実装計画書

## 1. プロジェクト概要

VERICUT に匹敵するリアルタイム切削シミュレーションアプリケーションを構築する。
STL ベースのストック・最終形状を入力とし、工具による材料除去をアニメーションで可視化。
最終的には、ストック・最終形状・工具群から最適な加工戦略を自動生成する機能を実現する。

### ゴール（段階的）

| フェーズ | 目標 | 成果物 |
|---------|------|--------|
| Phase 1 | 基盤構築 | STL読込・3D表示・Dexelモデル・基本工具定義 |
| Phase 2 | 切削シミュレーション | 材料除去エンジン・ツールパス実行・リアルタイムアニメーション |
| Phase 3 | 可視化・UI強化 | インタラクティブUI・断面表示・差分比較・削り残し表示 |
| Phase 4 | 解析・最適化 | 加工力推定・送り速度最適化・表面品質予測 |
| Phase 5 | 自動工程計画 | 特徴認識・工具選定・加工戦略自動生成・段取り計画 |

---

## 2. アーキテクチャ

### 2.1 全体構成

```
┌─────────────────────────────────────────────────────┐
│                  Browser (Frontend)                   │
│                                                       │
│  ┌─────────────┐  ┌──────────────┐  ┌─────────────┐ │
│  │  React UI    │  │  Three.js    │  │ Animation   │ │
│  │  Controls    │  │  3D Viewer   │  │ Controller  │ │
│  └──────┬──────┘  └──────┬───────┘  └──────┬──────┘ │
│         │                │                  │         │
│  ┌──────┴──────────────┴─────────────────┴──────┐   │
│  │           State Management (Zustand)           │   │
│  └──────────────────┬────────────────────────────┘   │
│                     │                                 │
│  ┌──────────────────┴────────────────────────────┐   │
│  │         WebAssembly Simulation Engine          │   │
│  │  ┌──────────┐ ┌──────────┐ ┌───────────────┐ │   │
│  │  │ Tri-Dexel│ │ G-code   │ │ Tool Swept    │ │   │
│  │  │ Model    │ │ Parser   │ │ Volume Calc   │ │   │
│  │  └──────────┘ └──────────┘ └───────────────┘ │   │
│  └───────────────────────────────────────────────┘   │
│                                                       │
└─────────────────────────────────────────────────────┘
                          │
                          │ REST / WebSocket
                          ▼
┌─────────────────────────────────────────────────────┐
│                Python Backend (FastAPI)               │
│                                                       │
│  ┌──────────────┐  ┌──────────────┐  ┌────────────┐ │
│  │ STL I/O      │  │ Toolpath     │  │ Feature    │ │
│  │ (stl-reader) │  │ Optimization │  │ Recognition│ │
│  └──────────────┘  └──────────────┘  └────────────┘ │
│                                                       │
│  ┌──────────────┐  ┌──────────────┐  ┌────────────┐ │
│  │ Process      │  │ Cutting Force│  │ Auto       │ │
│  │ Planning     │  │ Estimation   │  │ Strategy   │ │
│  └──────────────┘  └──────────────┘  └────────────┘ │
│                                                       │
└─────────────────────────────────────────────────────┘
```

### 2.2 技術スタック

| 層 | 技術 | 理由 |
|---|---|---|
| **フロントエンド** | React + TypeScript | コンポーネントベースUI、型安全性 |
| **3D描画** | Three.js (r182+) | CNC可視化エコシステムが充実、軽量 |
| **CSGブーリアン** | three-bvh-csg | BSP比100倍高速、Three.js公式サンプル採用 |
| **状態管理** | Zustand | 軽量、React外からもアクセス可能 |
| **シミュレーションコア** | Rust → WebAssembly | Tri-Dexelモデル、SIMD活用、ネイティブ級性能 |
| **G-codeパーサー** | gcode-parser (CNCjs) + Wasm | JS側で軽量パース、大容量はWasm |
| **バックエンド** | Python + FastAPI | 数値計算エコシステム (numpy/scipy) |
| **STL読込** | stl-reader (Python) / STLLoader (Three.js) | 高速パース |
| **最適化** | NSGA-II (pymoo) | 多目的最適化（時間・品質・エネルギー） |
| **特徴認識** | GNN or CNN (PyTorch) | メッシュからの加工フィーチャ検出 |
| **ビルド** | Vite | 高速HMR、Wasm対応 |

---

## 3. データモデル

### 3.1 Tri-Dexel モデル（材料表現の核心）

```
Tri-Dexelモデル: 3軸方向からの独立したDexelグリッド

X方向Dexelグリッド:        Y方向Dexelグリッド:        Z方向Dexelグリッド:
  Y                          X                          X
  ↑  ||||||||||||            ↑  ||||||||||||            ↑  ||||||||||||
  |  ||||||||||||            |  ||||||||||||            |  ||||||||||||
  |  ||||||||||||            |  ||||||||||||            |  ||||||||||||
  +--------→ Z              +--------→ Z              +--------→ Y

各Dexel = [(start₁, end₁), (start₂, end₂), ...] のセグメントリスト
```

**Dexelモデルの利点：**
- メモリ効率が高い（セグメントあたり64bit）
- 工具スウェプトボリュームとの交差計算が高速
- 業界標準（VERICUT、Mastercamが採用）
- Tri-Dexelにすることで等方性を改善

**解像度の目安：**
- 粗加工シミュレーション: 0.5mm グリッド間隔
- 仕上げシミュレーション: 0.05mm グリッド間隔
- 100mm × 100mm × 50mm のストック @ 0.1mm → 1000 × 1000 × 3軸 = 3M Dexel

### 3.2 工具モデル

```typescript
// 工具定義
interface Tool {
  id: string;
  name: string;
  type: 'flat' | 'ball' | 'bull_nose' | 'drill' | 'taper';
  diameter: number;        // mm
  cornerRadius: number;    // mm (bull_nose用、ball: diameter/2)
  fluteLength: number;     // mm
  shankDiameter: number;   // mm
  totalLength: number;     // mm
  numberOfFlutes: number;
  material: 'carbide' | 'hss' | 'ceramic' | 'diamond';
}

// 工具形状の断面プロファイル
//
// フラットエンドミル:     ボールエンドミル:     ラジアスエンドミル:
//    ┌──────┐              ┌──────┐              ┌──────┐
//    │      │              │      │              │      │
//    │      │              │      │              │      │
//    │      │              │      │              │  ╭──╮│
//    └──────┘              ╰──────╯              └──╯──╰┘
//    R=0                   R=D/2                 0<R<D/2
```

### 3.3 ツールパス

```typescript
interface ToolpathPoint {
  x: number; y: number; z: number;  // 工具先端位置
  a?: number; b?: number; c?: number; // 回転軸（5軸用）
  feedRate: number;                    // mm/min
  spindleSpeed: number;               // rpm
  type: 'rapid' | 'linear' | 'arc_cw' | 'arc_ccw';
}

interface ToolpathSegment {
  toolId: string;
  points: ToolpathPoint[];
  operation: 'roughing' | 'semi_finishing' | 'finishing';
}
```

---

## 4. フェーズ別実装詳細

### Phase 1: 基盤構築（2-3週間）

#### 1.1 プロジェクトセットアップ
```
cutting-simulator/
├── frontend/                  # React + TypeScript + Vite
│   ├── src/
│   │   ├── components/        # UIコンポーネント
│   │   │   ├── Viewer3D.tsx         # Three.js 3Dビューワー
│   │   │   ├── ToolPanel.tsx        # 工具設定パネル
│   │   │   ├── SimulationControls.tsx # 再生・停止・速度制御
│   │   │   └── FileLoader.tsx       # STL/G-codeファイル読込
│   │   ├── engine/            # シミュレーションエンジン（JS側）
│   │   │   ├── DexelModel.ts        # Dexelモデルラッパー
│   │   │   ├── ToolGeometry.ts      # 工具ジオメトリ生成
│   │   │   ├── GCodeParser.ts       # G-codeパーサー
│   │   │   └── SimulationRunner.ts  # シミュレーション実行制御
│   │   ├── store/             # Zustand ストア
│   │   ├── types/             # TypeScript型定義
│   │   └── utils/             # ユーティリティ
│   └── package.json
├── wasm-engine/               # Rust → WebAssembly
│   ├── src/
│   │   ├── lib.rs                   # Wasmエントリポイント
│   │   ├── dexel.rs                 # Tri-Dexelデータ構造
│   │   ├── tool.rs                  # 工具スウェプトボリューム
│   │   ├── boolean_ops.rs           # ブーリアン演算
│   │   └── stl_parser.rs            # STLパーサー
│   └── Cargo.toml
├── backend/                   # Python FastAPI
│   ├── app/
│   │   ├── main.py
│   │   ├── routers/
│   │   ├── services/
│   │   └── models/
│   └── requirements.txt
└── README.md
```

#### 1.2 STL読込・メッシュ表示
- Three.js STLLoader でSTLファイルをブラウザで読込
- ストック形状と最終形状を並べて/重ねて表示
- OrbitControls によるインタラクティブな視点操作
- メッシュの半透明表示・ワイヤーフレーム切替

#### 1.3 STL → Tri-Dexel 変換（Rust/Wasm）
- STLメッシュのバウンディングボックス計算
- 3軸方向のレイキャスティングでDexelセグメント生成
- Möller–Trumbore法によるレイ-三角形交差判定
- グリッド解像度の動的設定

#### 1.4 工具ジオメトリ
- フラットエンドミル（円柱）
- ボールエンドミル（半球＋円柱）
- ラジアスエンドミル（トーラス＋円柱）
- Three.js でのプレビュー表示

**Phase 1 成果物:** STLファイルを読み込んで3D表示できる。内部的にTri-Dexelモデルに変換済み。工具形状を選択・表示可能。

---

### Phase 2: 切削シミュレーション（3-4週間）

#### 2.1 材料除去エンジン（核心アルゴリズム）

```
材料除去の手順:

1. ツールパスの各セグメント(P₁→P₂)に対して:
   a. 工具スウェプトボリュームを計算
      - 工具を P₁ から P₂ まで直線移動した際の包絡体
   b. 影響範囲のDexelグリッドセルを特定（AABB）
   c. 各影響Dexelに対して:
      - スウェプトボリュームとの交差区間を計算
      - Dexelセグメントリストから交差区間を減算
      - 空になったセグメントを除去

工具スウェプトボリューム（直線移動）:
  ┌─────────────────────────┐
  │  ●═══════════════════●  │  ← 工具中心の軌跡
  │ /                     \ │
  │/   ミンコフスキー和     \│  ← 軌跡 ⊕ 工具断面
  └─────────────────────────┘
```

#### 2.2 G-code パーサー・インタプリタ
- G0 (早送り), G1 (直線補間), G2/G3 (円弧補間)
- モーダル状態管理（座標系、送り速度モード等）
- G54-G59 ワーク座標系
- M3/M4/M5 スピンドル制御
- T/M6 工具交換

#### 2.3 シミュレーション実行・アニメーション

```typescript
// アニメーションループの概要
class SimulationRunner {
  private dexelModel: WasmDexelModel;
  private toolpath: ToolpathSegment[];
  private currentStep: number = 0;
  private playbackSpeed: number = 1.0;

  tick(deltaTime: number) {
    // 1. 現在のステップに基づき工具位置を計算
    const toolPosition = this.interpolatePosition(deltaTime);

    // 2. Wasmエンジンで材料除去を実行
    this.dexelModel.subtractToolSwept(
      prevPosition, toolPosition, currentTool
    );

    // 3. 変更されたDexel領域のメッシュを再生成
    const updatedMesh = this.dexelModel.generateMesh(changedRegion);

    // 4. Three.jsシーンを更新
    this.updateScene(updatedMesh, toolPosition);
  }
}
```

#### 2.4 Dexel → メッシュ変換（表示用）
- Marching Cubes 的手法でDexelからサーフェスメッシュを生成
- 差分更新：変更されたグリッド領域のみ再メッシュ化
- LOD（Level of Detail）：カメラ距離に応じた詳細度制御

**Phase 2 成果物:** G-codeを読み込み、ストックが時々刻々削られるアニメーションを表示。再生・一時停止・速度変更が可能。

---

### Phase 3: 可視化・UI強化（2-3週間）

#### 3.1 高度な可視化
- **削り残し/過切削のカラーマップ表示**
  - 最終形状との差分を色で表示（青=削り残し、赤=過切削）
  - 許容値（トレランス）設定と合否判定
- **断面表示**
  - 任意平面でのクリッピングプレーン
  - 断面のプロファイル線表示
- **工具軌跡の軌跡線表示**
  - 早送り（赤点線）/ 切削送り（青実線）の色分け

#### 3.2 インタラクティブUI
```
┌────────────────────────────────────────────────────────┐
│ [📂 Open] [▶ Play] [⏸ Pause] [⏭ Step] [Speed: 10x ▼] │
├──────────────────────────────┬─────────────────────────┤
│                              │  📋 工具リスト           │
│                              │  ├ T1: φ10 フラット     │
│                              │  ├ T2: φ6 ボール        │
│     3D Viewer                │  └ T3: φ8 R1 ラジアス   │
│     (Three.js)               │─────────────────────────│
│                              │  📊 シミュレーション情報  │
│                              │  残材料量: 45.2%         │
│                              │  加工時間: 12:34         │
│                              │  現在行: N1250 G1 X...   │
├──────────────────────────────┤─────────────────────────│
│  タイムライン ───●──────────  │  ⚙ 設定                 │
│  [T1 荒加工][T2 中仕上][T3] │  解像度: 0.1mm          │
└──────────────────────────────┴─────────────────────────┘
```

#### 3.3 追加機能
- STL/OBJ エクスポート（シミュレーション結果の保存）
- スクリーンショット・動画録画
- 複数ビュー（正面・側面・上面 + パース）

**Phase 3 成果物:** 完成度の高いGUIで直感的にシミュレーションを操作可能。削り残しの定量的評価が可能。

---

### Phase 4: 解析・最適化（3-4週間）

#### 4.1 切削条件解析
- **切削力推定モデル**
  - 各ステップでの切り込み断面積（Dexelから計算可能）
  - 比切削抵抗に基づく切削力推定
  - 切削力の時間変化グラフ表示

```
切削力 F = Kc × Ac × (材料係数)

Kc: 比切削抵抗 [N/mm²]
Ac: 切り込み断面積 [mm²] ← Dexelの差分から算出

      切り込み断面積
      ┌──────────┐
      │ ████████ │  ← 1ステップで除去される断面
      │ ████████ │
      └──────────┘
```

#### 4.2 送り速度最適化
- **目的:** 一定の切削力（負荷）を維持するよう送り速度を自動調整
- **アルゴリズム:**
  1. 全ツールパスについて各ステップの除去体積を事前計算
  2. 目標切削力を設定
  3. 各ステップの除去体積に反比例して送り速度を調整
  4. 機械の加減速能力を考慮した平滑化
- **効果:** 加工時間の短縮（空切り時の加速）と工具寿命の延長

#### 4.3 多目的最適化（NSGA-II）

```
最適化変数:            目的関数（最小化）:
┌──────────────────┐   ┌──────────────────┐
│ 切削速度 Vc      │   │ 加工時間         │
│ 送り速度 Vf      │   │ 工具摩耗量       │
│ 切り込み量 ap    │──→│ 表面粗さ         │
│ ピッチ ae        │   │ エネルギー消費量 │
│ 工具選択         │   │ コスト           │
└──────────────────┘   └──────────────────┘
                        ↓
                   パレート最適解群
```

#### 4.4 衝突・干渉検出
- 工具ホルダー・シャンク部とワーク/治具との干渉チェック
- 機械可動範囲の制約チェック
- 警告の視覚的表示（干渉箇所を赤くハイライト）

**Phase 4 成果物:** 切削力グラフ表示、送り速度の自動最適化、衝突検出警告機能。

---

### Phase 5: 自動工程計画（4-6週間）

#### 5.1 フィーチャ認識

```
入力STL（最終形状）から加工フィーチャを自動検出:

最終形状 STL
    ↓
メッシュセグメンテーション
    ↓  （面の曲率・法線方向でクラスタリング）
面グループ分類
    ↓  （平面/円筒面/自由曲面）
フィーチャ検出
    ↓
┌──────────┬──────────┬──────────┬──────────┐
│ ポケット  │ 穴       │ スロット  │ 自由曲面  │
│ (pocket) │ (hole)   │ (slot)   │ (surface)│
│          │ ├ 貫通穴  │          │          │
│          │ └ 止まり穴│          │          │
└──────────┴──────────┴──────────┴──────────┘
```

**手法:** GNN（グラフニューラルネットワーク）ベースの分類器
- 入力: STLメッシュの面隣接グラフ + 面属性（法線、曲率、面積）
- 出力: 各面のフィーチャラベル
- 学習データ: 合成CADモデルから自動生成

#### 5.2 加工戦略自動生成

```
フィーチャ群 + ストック形状 + 工具群
    ↓
┌─────────────────────────────────────────┐
│           加工戦略プランナー              │
│                                          │
│  1. 加工方向の決定                        │
│     └ ストック vs 最終形状の差分解析     │
│     └ アンダーカットの検出               │
│     └ 必要なセットアップ数の最小化       │
│                                          │
│  2. 工程分割                             │
│     └ 荒加工 → 中仕上げ → 仕上げ       │
│     └ フィーチャ間の加工順序決定         │
│     └ 工具交換回数の最小化               │
│                                          │
│  3. 各工程の詳細設定                     │
│     └ 最適工具の選択                     │
│     └ 切削条件の決定（被削材に応じて）   │
│     └ ツールパスパターンの選択           │
│        ├ ジグザグ（荒加工）              │
│        ├ 等高線（側面仕上げ）            │
│        ├ 走査線（底面仕上げ）            │
│        └ スパイラル（ポケット）           │
│                                          │
│  4. ツールパス生成                        │
│     └ 各パターンに応じた工具位置列生成   │
│     └ 安全高さ・アプローチ/リトラクト    │
│     └ G-codeへの変換                     │
│                                          │
└─────────────────────────────────────────┘
    ↓
加工計画書 + G-code + 推定加工時間
```

#### 5.3 段取り計画

- **セットアップ（取り付け方向）の最適化**
  - 最小セットアップ数で全フィーチャにアクセス可能な方向集合を求める
  - 集合被覆問題としてモデル化
- **治具設計のガイダンス**
  - 各セットアップでの固定面の推奨
  - クランプ位置と工具パスとの干渉チェック
- **加工順序の最適化**
  - 精度要求の高いフィーチャ間の加工順序制約
  - 残留応力・変形を考慮した順序決定

#### 5.4 LLMアシスタント統合（将来拡張）

- 自然言語での加工条件相談
  - 「アルミ合金A5052をφ10のボールエンドミルで仕上げたい。推奨条件は？」
- 加工計画の自然言語での説明生成
- 異常検知時のアドバイス生成

**Phase 5 成果物:** STLと工具群を入力すると、加工戦略・G-code・推定時間が自動生成される。

---

## 5. 性能目標

| 指標 | 目標値 | 備考 |
|------|--------|------|
| フレームレート | ≥ 30 FPS | アニメーション中 |
| Dexel解像度 | 0.05mm〜0.5mm | 用途に応じて可変 |
| 1ステップの除去計算 | < 5ms | SIMD活用Wasm |
| STL読込 (10MB) | < 2秒 | stl-reader使用 |
| G-codeパース (100万行) | < 3秒 | ストリーミング |
| メッシュ再生成 (差分) | < 16ms | 変更領域のみ |

---

## 6. 主要アルゴリズム詳細

### 6.1 STL → Dexel 変換

```
アルゴリズム: STL-to-Dexel変換

入力: STLメッシュ（三角形リスト）, グリッド解像度 δ
出力: Tri-Dexelモデル

Z方向Dexelグリッドの生成手順:
1. バウンディングボックス [xmin,xmax] × [ymin,ymax] × [zmin,zmax] を計算
2. グリッド: nx = ceil((xmax-xmin)/δ), ny = ceil((ymax-ymin)/δ)
3. for each グリッドセル (i, j):
     a. レイ原点 = (xmin + i*δ + δ/2, ymin + j*δ + δ/2, zmin - ε)
     b. レイ方向 = (0, 0, 1)
     c. 全三角形とレイの交差点を求める（Möller–Trumbore法）
     d. 交差点をZ値でソート
     e. ペアにしてDexelセグメント化: [(z₁,z₂), (z₃,z₄), ...]
4. X方向、Y方向も同様に実行（Tri-Dexel）
```

### 6.2 工具スウェプトボリュームとDexelの交差

```
アルゴリズム: 工具スウェプト − Dexel減算

入力: 工具形状T, 移動 P₁→P₂, Dexelグリッド
出力: 更新されたDexelグリッド

1. 移動ベクトル v = P₂ - P₁
2. スウェプトボリュームのAABB計算
3. AABB内の影響Dexelセルを列挙
4. for each 影響Dexelセル (i, j):
     a. Dexelレイと工具スウェプト体の交差区間を計算
        - 工具タイプに応じた解析解を使用:
          * フラット: 円柱のスウェプト（角柱+半円柱）
          * ボール:   球のスウェプト（カプセル形状）
          * ラジアス: トーラスのスウェプト
     b. 交差区間 [za, zb] を Dexelセグメントリストから減算
        - セグメント [s, e] と [za, zb] の関係:
          * 完全内包: セグメント削除
          * 部分重複（上側）: e → za に短縮
          * 部分重複（下側）: s → zb に短縮
          * 中間分割: [s, za] と [zb, e] に分割
```

### 6.3 Dexel → 表示メッシュ変換

```
手法: Dexelトップサーフェスの直接メッシュ化

1. Z-Dexelグリッドの各セルの最上面(top)と最下面(bottom)を取得
2. 隣接セル間の高さ差から側面ポリゴンを生成:
   - cell(i,j).top ≠ cell(i+1,j).top → 側面四角形を生成
3. 上面: 各セルの上面を2三角形で構成
4. 法線計算: 隣接セルの高さ差から近似法線を算出

最適化:
- 変更フラグ付きセルのみ再メッシュ化
- BufferGeometryの部分更新（setDrawRange, 属性部分書換え）
```

---

## 7. 開発ロードマップ

```
Month 1          Month 2          Month 3          Month 4          Month 5-6
┌──────────┐    ┌──────────┐    ┌──────────┐    ┌──────────┐    ┌──────────┐
│ Phase 1   │    │ Phase 2   │    │ Phase 2  │    │ Phase 3   │    │ Phase 4  │
│           │    │           │    │ (cont.)  │    │           │    │          │
│ 基盤構築   │───▶│ 切削      │───▶│ アニメ   │───▶│ UI強化    │───▶│ 解析     │
│ STL読込   │    │ エンジン   │    │ ーション │    │ 可視化    │    │ 最適化   │
│ Dexel     │    │ G-code    │    │ 完成     │    │ 完成      │    │          │
│ 工具定義   │    │ パーサー   │    │          │    │           │    │          │
└──────────┘    └──────────┘    └──────────┘    └──────────┘    └──────────┘
                                                                       │
                                                                       ▼
                                                                 ┌──────────┐
                                                                 │ Phase 5  │
                                                                 │ 自動     │
                                                                 │ 工程計画  │
                                                                 │ (2ヶ月)  │
                                                                 └──────────┘
```

---

## 8. リスクと対策

| リスク | 影響 | 対策 |
|--------|------|------|
| Dexel計算のリアルタイム性能不足 | アニメーションがカクつく | Rust/Wasm SIMD最適化、差分更新、LOD |
| 大容量STLファイルのメモリ不足 | ブラウザクラッシュ | ストリーミングパース、Octree併用 |
| Tri-Dexelの精度不足（自由曲面） | 仕上げ面の品質低下 | 適応的解像度、仕上げは高解像度Dexel |
| G-code方言への対応 | 特定機械のG-codeが読めない | プラグイン式ポストプロセッサ |
| 5軸加工への対応の複雑さ | 開発遅延 | 初期は3軸に集中、段階的に拡張 |
| フィーチャ認識の精度 | 誤った加工計画 | 人間のレビュー・修正機能を必ず付与 |

---

## 9. 初期スコープ（MVP）

最小限の実用的プロダクト（MVP）として、以下を Phase 1-2 で実現する：

1. **STLファイル2つ（ストック + 最終形状）を読み込んで3D表示**
2. **G-codeファイルを読み込んでツールパスを表示**
3. **フラット/ボール/ラジアスエンドミルの3種の工具定義**
4. **再生ボタンでストックが削れるアニメーション**
5. **最終形状との差分をカラー表示**

これだけで「VERICUTライク」な基本体験が得られる。

---

## 10. 次のアクション

Phase 1 の実装を開始する：

1. `cutting-simulator/frontend/` の Vite + React + TypeScript プロジェクト初期化
2. `cutting-simulator/wasm-engine/` の Rust + wasm-pack プロジェクト初期化
3. Three.js による STL ビューワーの実装
4. Rust で Tri-Dexel データ構造の実装
5. STL → Tri-Dexel 変換の実装

---

*本計画書は開発の進行に応じて更新される。*
